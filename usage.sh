./diag_revealer
    DIAG_CFG                path to a config file specifying the types of messages to recieve. see https://github.com/Chris-Wimtech/mobileinsight-core/tree/reverse_engineering/dm_collector_c
    FIFI_PATH               path to the fi/fo pipe used to communicate between diage_revealer and APP code.
    [LOG_OUTPUT_DIR]        optional path to a directory to dump logs.
    [LOG_CUT_SIZE (in MB)]  optional maximum size at which to slice logs. No logs larger than this will be produced.
